import { MovieTransformPipe } from './movie-transform.pipe';

describe('MovieTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new MovieTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
